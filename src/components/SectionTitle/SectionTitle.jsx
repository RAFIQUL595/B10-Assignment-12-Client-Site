import React from 'react';

const SectionTitle = ({ heading }) => {
    return (
        <div>
            <h3 className="text-3xl text-center font-bold mb-10">{heading}</h3>
        </div>
    );
};

export default SectionTitle;